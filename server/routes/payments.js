const express = require('express');
const router = express.Router();

// Public payment routes are not used — all payments are managed through the admin portal.
// This file is kept for potential future public-facing features.

module.exports = router;
